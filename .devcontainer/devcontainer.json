{
  "name": "brickbybrick-fix",
  "containerUser": "ros2",
  "initializeCommand": [
    "docker",
    "build",
    "--file=aica-package.toml",
    "--target=development",
    "--build-arg=config.signing.enabled=false",
    "--tag=aica-technology/brickbybrick-fix:development",
    "."
  ],
  "image": "aica-technology/brickbybrick-fix:development",
  "postStartCommand": "rm -r /home/ros2/ws/src/brickbybrick_fix && ln -s ${containerWorkspaceFolder}/source/brickbybrick_fix /home/ros2/ws/src/brickbybrick_fix",
  "postCreateCommand": "/usr/local/bin/generate_compile_commands -p brickbybrick_fix -t 60 -o ${containerWorkspaceFolder} --cmake-args BUILD_TESTING=ON",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-vscode.cpptools-extension-pack",
        "llvm-vs-code-extensions.vscode-clangd",
        "xaver.clang-format",
        "charliermarsh.ruff"
      ]
    }
  }
}